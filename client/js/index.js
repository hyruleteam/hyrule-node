import {getProductInfo, confirmOrder, searchHistory, connectNetwork} from './api/api'

var index = new Vue({
    el: '#v-index',
    data: {
        productInfo: [],
    },
    filters: {},
    computed: {},
    created() {},
    methods: {}
})
